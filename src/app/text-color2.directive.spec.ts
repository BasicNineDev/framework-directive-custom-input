import { TextColor2Directive } from './text-color2.directive';

describe('TextColor2Directive', () => {
  it('should create an instance', () => {
    const directive = new TextColor2Directive();
    expect(directive).toBeTruthy();
  });
});
